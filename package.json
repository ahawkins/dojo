{
	"name": "dojo",
	"version": "1.0.0",
	"description": "A place for my programming katas, exercises, and practice.",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"prepare": "husky install"
	},
	"license": "ISC",
	"devDependencies": {
		"husky": "^8.0.3",
		"lint-staged": "^13.2.2",
		"prettier": "^2.8.8",
		"prettier-plugin-sh": "^0.12.8"
	}
}
